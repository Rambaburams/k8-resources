apiVersion: v1
kind: Pod
metadata:
  name: ngnix
  namespace : roboshop
  labels:
    environment: development
    project: roboshop
spec:
  containers:
  - name: ngnix
    image: ngnix:latest
    ports:
    - containerPort: 80
